'use strict';

describe('myApp.gameHistory module', function() {

  beforeEach(module('myApp.gameHistory'));

  describe('gameHistory controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var gameHistoryCtrl = $controller('GameHistoryCtrl');
      expect(gameHistoryCtrl).toBeDefined();
    }));

  });
});