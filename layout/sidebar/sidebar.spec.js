'use strict';

describe('myApp.sidebar module', function() {

  beforeEach(module('myApp.sidebar'));

  describe('sidebar controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var sidebarCtrl = $controller('SidebarCtrl');
      expect(sidebarCtrl).toBeDefined();
    }));

  });
});