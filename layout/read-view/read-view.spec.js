'use strict';

describe('myApp.readView module', function() {

  beforeEach(module('myApp.readView'));

  describe('readView controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var readViewCtrl = $controller('ReadViewCtrl');
      expect(readViewCtrl).toBeDefined();
    }));

  });
});