'use strict';

describe('myApp.gameCard module', function() {

  beforeEach(module('myApp.gameCard'));

  describe('gameCard controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var gameCardCtrl = $controller('GameCardCtrl');
      expect(gameCardCtrl).toBeDefined();
    }));

  });
});